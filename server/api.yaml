http:
  port: 4251
  main: "localhost"

localhost:
  path: '../'

ws:
  port: 4252

https:
  port: 443

password: "abc"

production: false
modules:
  - functions
  - query
  - site
  - socket
  - fs
  - proxy

loadSet:
  - lastSync

fs:
  collection: 'files'
  allowed:
    - '/tmp/duke/pix8_data'
  folder:
    item: 'pix8.json'

path:
  modules: './modules/'
  files: './upl/'
  tmp: './tmp/'
  ffmpeg: ""
  ffprobe: ""
  youtube: './tube/'
  certificates: 'certificates/'

devMode: true
currency: EUR
lang: en

key: mk

wait: false
